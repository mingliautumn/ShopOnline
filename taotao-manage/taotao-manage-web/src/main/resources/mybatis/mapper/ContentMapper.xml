<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- <!DOCTYPE mapper -->
<!--   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" -->
<!--   "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> -->

<mapper namespace="com.taotao.manage.mapper.ContentMapper">
	<select id="queryListOrderByUpdateDesc" resultType="Content">
		select * from tb_content t where t.category_id = #{categoryId} order by updated DESC
	</select>
	
</mapper>